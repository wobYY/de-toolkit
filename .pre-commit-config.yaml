repos:
- repo: https://github.com/astral-sh/ruff-pre-commit
  # Ruff version.
  rev: v0.11.2
  hooks:
    # Run the formatter.
    - id: ruff-format
      args: [--respect-gitignore]
    # Run the linter.
    - id: ruff
      args: [ --fix ]